# Session Brief — VaiBReport

<!-- Generated by chaos-controller. Do not edit manually. -->

## Unfinished Threads

- HTTPS not yet enforced on report.vaibos.com — GitHub Let's Encrypt cert still issuing.
  → Next: check `gh api repos/CHAOSAiGENT/VaiBReport/pages --jq .https_certificate.state`; when "approved", `gh api -X PUT .../pages -f https_enforced=true`. Poller `btfu5uk4z` may have already done it.
- CRLF/LF churn causes spurious diffs and merge conflicts.
  → Next: add `.gitattributes` at repo root with `* text=auto eol=lf` (and `*.png binary` etc.).
- ~15 pipeline workflows still on Node-20 actions (forced cutover 2026-06-16).
  → Next: bump `actions/checkout@v4→v6`, `upload-artifact`, etc. across `.github/workflows/fetch-*.yml`, `generate-digest.yml`.
- External auto-sync daemon races manual commits (auto-commits+pushes main).
  → Next: locate the daemon (not in repo); pause it before multi-commit sequences.
- Redesign content gaps: compare-to backfill + feature-flag-empty-data heroes render nothing.
  → Next: tasks/nvidia-cascade-and-backfill.md — swap dead NIM model IDs, fix blurb batch size.

## Stack State

- Live site: Next.js 16.2.6 static export (App Router, Turbopack, React 19, Tailwind v4) on GitHub Pages.
- Custom domain: report.vaibos.com (root; basePath dropped; CNAME → chaosaigent.github.io).
- Content read by `next/src/lib/content.ts` via `CONTENT_ROOT` (defaults to repo root; workflow sets it to `github.workspace`).
- Pkg manager (next/): Bun 1.2.18 (`bun.lock` tracked). Deploy: `.github/workflows/deploy-site.yml`.
- Data pipeline: Node >=20, zero-dep ESM, 8 daily fetch workflows → `data/` + `_posts/` + `_repos/`.
- Secrets: none handled this session (LLM cascade + data APIs via Actions secrets, untouched).
- No MCP registry (pruned). External auto-sync daemon active.

## Recent Decisions

- 2026-06-05: report.vaibos.com custom domain, immediate cutover.
- 2026-06-05: Next.js redesign replaces Jekyll as the Pages deploy (`deploy-blog.yml` removed).
- 2026-06-05: `S:\Chaos_Skunkworks\Apps\VaiBReport` is the single canonical clone (`__platforms` mirror deleted).
- Gotcha: auto-sync daemon auto-commits+pushes main on a timer — rebase over it.
- Gotcha: remark-html@16 sanitizes by default (XSS finding was a false positive).
